<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">
    <resultMap id="user" type="com.example.domain.User">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="age" property="age" jdbcType="INTEGER"/>
        <result column="ctm" property="ctm" jdbcType="TIMESTAMP"/>
        <result column="contact_address" property="contactAddress" jdbcType="VARCHAR" />
    </resultMap>

    <sql id="Base_Column_List">
        id, username, age, ctm, contact_address
    </sql>


    <select id="getUserByName" parameterType="String" resultMap="user">
        SELECT
        <include refid="Base_Column_List"/>
        FROM tb_user
        WHERE username = #{username}
    </select>

    <select id="getUsers" resultMap="user">
        SELECT
        <include refid="Base_Column_List"/>
    FROM tb_user

    </select>

    <select id="getAll"   resultMap="user">
        SELECT <include refid="Base_Column_List"/>
        FROM tb_user
        <if test="user != null">
            WHERE contact_address = #{user.contactAddress}
            AND age = #{user.age}
            AND  username = #{user.username}

        </if>

    </select>

    <select id="getUserByAge" resultMap="user">
        SELECT <include refid="Base_Column_List"/>
        FROM tb_user
        <if test="#{age} != null">
            WHERE age = #{age}
        </if>

    </select>
</mapper>